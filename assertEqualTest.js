const assertEqual = require('../assertEqual');
const assert = require('chai').assert;


describe("#assertEqual", () => {

  it("return Lighthouse Labs for[Lighthouse Labs]", () => {
    assert.strictEqual(assertEqual(["Lighthouse Labs"]), "Lighthouse Labs");

  });

  



});